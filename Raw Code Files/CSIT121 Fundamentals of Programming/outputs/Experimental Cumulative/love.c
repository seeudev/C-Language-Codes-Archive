#include<stdio.h>

void printNumbers(int N)
{
    for (int i = N; i = N; i++)
    
}

int main()
{
    int num;
    printf("Enter an integer: "); scanf("%d",&num);
    printNumbers(num);
    
}
